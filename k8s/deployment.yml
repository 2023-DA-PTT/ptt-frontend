apiVersion: apps/v1
kind: Deployment
metadata:
  name: ptt-frontend
  namespace: ptt
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ptt-frontend
  template:
    metadata:
      labels:
        app: ptt-frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/2023-da-ptt/ptt-frontend:latest
          #imagePullPolicy: Always
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
            limits:
              memory: "128Mi"
              cpu: "500m"
      imagePullSecrets:
        - name: dockerconfigjson-github-com